# minimal-sip-stateless-proxy

> A SIP Stateless Proxy implementation in Rust

https://www.rfc-editor.org/rfc/rfc3261.txt

## Installation

```sh
git clone https://github.com/bhbs/minimal-sip-stateless-proxy.git
cd minimal-sip-stateless-proxy
```

## Run

```sh
cargo run <host> <port>
```
